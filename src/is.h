#pragma once

#include <string>
#include <fstream>

using namespace std;

bool isSpace(char tmp);
bool isDigital(char tmp);
bool isAlpha(char tmp);
char skipSpace(char p);
void inputWord();
void inputNum();